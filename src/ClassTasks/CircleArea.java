package ClassTasks;

public class CircleArea {
    public static void main(String[] args) {
        double radius = 5.6;
        double circleArea = Math.PI * Math.pow(radius, 2);
        System.out.printf("Circle area is %f", circleArea);
    }
}
